pipeline {
    agent any

    stages {
        stage('Unit TESTS - bACKEND') {
            steps {
                echo 'Hello World'
                dir('bugtracker-backend'){
                    go test ./... -v
                }
            }
        }
    }
}
