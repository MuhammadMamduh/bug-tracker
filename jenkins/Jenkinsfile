pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            steps {
                echo 'Hello World'
                dir('bugtracker-backend'){
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
