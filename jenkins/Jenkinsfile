pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            agent {
                docker {
                    image 'snakee/golang-junit:1.21'
                    reuseNode true
                }
            }
            steps {
                echo 'Hello World'
                dir('bugtracker-backend'){
                    sh 'sudo su'
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
